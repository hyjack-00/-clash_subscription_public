# Release Channel

This repository exposes generated Clash-compatible subscription artifacts.

## Public Paths

- `dist/config.yaml`
- `dist/metadata.json`

## Update Model

- artifacts are updated by an external private workflow
- public output keeps the same stable `dist/` path
- repository scaffolding is kept lightweight on purpose
